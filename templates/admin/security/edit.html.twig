{% extends 'admin/layout.html.twig' %}

{% set page_title = 'Bewerk account' ~ (account.person ? ' van ' ~ account.person.canonical : '' %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button-group">
                <button type="submit">Opslaan</button>
                <a href="{{ path('admin_security_show', { 'id': account.id }) }}">Annuleren</a>
            </div>
        {{ form_end(form) }}
    </div>

{% endblock %}