{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Personen' %}
    
    {% block buttons %}
    <a href="{{ path("admin_person_new") }} ">Nieuw</a>
    {% endblock %}
{% endif %}

{% block body_wrap %}
    <div class="tabs">
        <input type="radio" name="tabs" id="main" checked>
        <label for="main" class="tab">
            <span>Overzicht</span>
        </label>
        <div class="panel">
            <div class="card">
                {% include 'admin/person/index/overview.html.twig' %}
            </div>
        </div>

        <input type="radio" name="tabs" id="schemes">
        <label for="schemes" class="tab">
            <span>Schema's</span>
        </label>
        <div class="panel">
            <div class="card">
                {% include 'admin/person/index/schemes.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}