{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Schema type ' ~ default.name %}
    
    {% block buttons %}
        <a href="{{ path("admin_document_scheme_new_selected", { 'id': default.id }) }} ">Nieuwe scheme</a>
        <a href="{{ path('admin_document_scheme_default_edit', { 'id': default.id }) }}">Bewerken</a>
        <a href="{{ path('admin_document_scheme_delete', { 'id': default.id }) }}">Verwijderen</a>
        <a href="{{ path("admin_document_scheme_index") }} ">Terug</a>
    {% endblock %}
{% endif %}

{% block body_wrap %}
    <div class="tabs">
        <input type="radio" name="tabs" id="overview" checked>
        <label for="overview" class="tab">
            <span>Overzicht</span>
        </label>
        <div class="panel">
            <div class="card">
                {% include 'admin/document/schemeDefault/show/overview.html.twig' %}
            </div>
        </div>

        <input type="radio" name="tabs" id="details">
        <label for="details" class="tab">
            <span>Details</span>
        </label>
        <div class="panel">
            <div class="card">
                {% include 'admin/document/schemeDefault/show/details.html.twig' %}
            </div>
        </div>
    </div>
{% endblock %}