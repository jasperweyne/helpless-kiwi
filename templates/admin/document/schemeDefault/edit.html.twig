{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Bewerk default schema type"' ~ scheme.name ~ '"' %}
{% endif %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button-group">
                <button type="submit" class="button add on">Opslaan</button>
                <a href="{{ path('admin_document_scheme_index') }}" class="button">Annuleren</a>
            </div>
        {{ form_end(form) }}
    </div>

{% endblock %}