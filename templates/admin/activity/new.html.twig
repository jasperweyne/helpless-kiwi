{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Nieuwe activiteit' %}
{% endif %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            {% include '@CraueFormFlow/FormFlow/buttons.html.twig' with {'craue_formflow_button_render_reset': false} %}
        {{ form_end(form) }}
        <br></br>
        * Dit veld is verplicht
    </div>
{% endblock %}
