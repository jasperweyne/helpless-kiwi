{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Nieuwe activiteit' %}
{% endif %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <p><span style="color: red">*</span> Dit veld is verplicht</p>
            {% include 'components/formflow_buttons.html.twig' with {'craue_formflow_button_render_reset': false} %}
        {{ form_end(form) }}
    </div>
{% endblock %}
