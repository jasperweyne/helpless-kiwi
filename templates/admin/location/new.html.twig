{% extends 'admin/layout.html.twig' %}

{% if page_title is not defined %}
    {% set page_title = 'Nieuwe locatie' %}
{% endif %}

{% block body %}
    <div class="form">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button-group">
                <button type="submit" class="button add icon">Toevoegen</button>
                <a href="{{ path('admin_location_index') }}" class="button secondary">Annuleren</a>
            </div>
        {{ form_end(form) }}
        <br></br>
        * Dit veld is verplicht
    </div>
{% endblock %}
